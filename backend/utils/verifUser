let models = require('../models');

module.exports = {
  
    isAdmin: function (value) {
        models.User.findOne({id: value})
        .then((user)=>{
    
            if (!user.isAdmin){
                return false;
            }
            else if(user.isAdmin){
                return true;
            }


        })
    }
}